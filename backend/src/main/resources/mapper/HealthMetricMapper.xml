<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.health.mapper.HealthMetricMapper">

    <select id="getTrendData" resultType="org.jeecg.modules.health.entity.HealthMetric">
        SELECT *
        FROM health_metric
        WHERE del_flag = 0
        AND patient_id = #{patientId}
        AND metric_type = #{metricType}
        <if test="startDate != null">
            AND record_date >= #{startDate}
        </if>
        <if test="endDate != null">
            AND record_date &lt;= #{endDate}
        </if>
        ORDER BY record_date ASC
    </select>

    <select id="getAbnormalMetrics" resultType="org.jeecg.modules.health.entity.HealthMetric">
        SELECT *
        FROM health_metric
        WHERE del_flag = 0
        AND patient_id = #{patientId}
        AND status = 'ABNORMAL'
        ORDER BY record_date DESC
    </select>

</mapper>
