# GitHub上传前检查清单

## ⚠️ 重要：上传前必须检查

### 1. 敏感信息检查

- [ ] **数据库密码**：`backend/src/main/resources/application.yml` 中的 `password` 已改为 `${DB_PASSWORD:your_password}`
- [ ] **JWT密钥**：`jwt.secret` 已改为 `${JWT_SECRET:your-secret-key-here}`
- [ ] **API密钥**：检查代码中是否有硬编码的API密钥
- [ ] **个人邮箱/电话**：检查文档中是否有个人信息

### 2. 配置文件检查

- [ ] `application.yml` 已使用环境变量占位符
- [ ] `application.yml.example` 已创建并包含说明
- [ ] `.gitignore` 已配置忽略 `application.yml`（包含敏感信息的实际配置文件）

### 3. 文件检查

- [ ] `node_modules/` 不会被提交（已在.gitignore中）
- [ ] `target/` 不会被提交（已在.gitignore中）
- [ ] `upload/` 目录不会被提交（已在.gitignore中）
- [ ] `logs/` 目录不会被提交（已在.gitignore中）

### 4. 文档检查

- [ ] README.md 已更新，包含前后端信息
- [ ] 文档中没有硬编码的密码或密钥
- [ ] 文档中的示例数据都是测试数据

---

## 🔍 快速检查命令

```bash
cd e:\Code\HealthManagementDashboard

# 1. 检查.gitignore是否生效
git status

# 2. 检查application.yml内容（确认没有真实密码）
cat backend/src/main/resources/application.yml | grep -i password

# 3. 检查是否有硬编码的密钥
grep -r "12345" backend/src/ --exclude-dir=target
grep -r "health-management-secret-key-2024" backend/src/ --exclude-dir=target

# 4. 查看将要提交的文件列表
git ls-files
```

---

## ✅ 确认无误后执行

```bash
# 1. 初始化Git（如果还没有）
git init

# 2. 添加文件
git add .

# 3. 提交
git commit -m "Initial commit: 医疗健康管理系统"

# 4. 添加远程仓库
git remote add origin https://github.com/YOUR_USERNAME/HealthManagementDashboard.git

# 5. 推送
git push -u origin main
```

---

## 🚨 如果发现敏感信息已提交

### 方法1：修改后重新提交（推荐）

```bash
# 1. 修改敏感信息
# 2. 重新提交
git add backend/src/main/resources/application.yml
git commit -m "fix: 移除敏感信息，使用环境变量"
git push
```

### 方法2：从历史中移除（如果已推送）

```bash
# 警告：这会重写Git历史，谨慎使用
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch backend/src/main/resources/application.yml" \
  --prune-empty --tag-name-filter cat -- --all

git push origin --force --all
```

---

**记住：安全第一！确认无误后再推送！**
